<!-- app/views/users/_user.html.erb -->
<% if !user.admin_user? %>
<tr id="user_<%= user.id %>">
  <td>
    <%= image_tag(current_user.profile_image_url, class: "rounded-circle", width: 50, height: 50) %>
  </td>

  <td><%= user.first_name %> <%= user.last_name %></td>
  <td><%= user.email %></td>
  <td><%= user.role.capitalize %></td>
  <td>
    <div class="d-flex justify-content-between">
      <%= link_to "View", user_path(user), class: "btn btn-info" %>
      <%# <%= link_to "Edit", edit_user_path(user), class: "btn btn-primary" %>  
      <%= button_to "Delete", user_path(user), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger", form: { onsubmit: "return confirm('Are you sure?');" } %>
    </div>
  </td>
</tr>

<% end %>
 