Swal.fire({
  title: "Invitation Sent!",
  text: "<%= j @user.first_name %> has been invited successfully.",
  icon: "success"
});

// Disable the button after sending the invite
document.getElementById("invite-user-<%= @user.id %>").disabled = true;
